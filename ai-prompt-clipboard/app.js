const grid=document.getElementById('grid'),search=document.getElementById('search'),cats=document.getElementById('cats'),empty=document.getElementById('empty'),theme=document.getElementById('theme'),themeText=document.getElementById('themeText'),toast=document.getElementById('toast');let active='All',timer;
const esc=s=>String(s).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
function categories(){return ['All',...new Set(defaultPrompts.map(x=>x.category))]}
function renderCats(){cats.innerHTML=categories().map(x=>`<button class="cat ${x===active?'active':''}" data-cat="${esc(x)}">${esc(x)}</button>`).join('');cats.querySelectorAll('.cat').forEach(b=>b.onclick=()=>{active=b.dataset.cat;renderCats();render()})}
const copySvg=()=>`<svg viewBox="0 0 24 24"><rect x="8" y="8" width="11" height="12" rx="2"/><path d="M16 8V6a2 2 0 0 0-2-2H6a2 2 0 0 0-2 2v10a2 2 0 0 0 2 2h2"/></svg>`;
function render(){let q=search.value.trim().toLowerCase(),list=defaultPrompts.filter(x=>(active==='All'||x.category===active)&&`${x.title} ${x.category} ${x.prompt}`.toLowerCase().includes(q));grid.innerHTML=list.map(x=>`<article class="card"><div class="cardhead"><h2 class="title">${esc(x.title)}</h2><span class="tag">${esc(x.category)}</span></div><pre class="prompt">${esc(x.prompt)}</pre><button class="copy" data-id="${esc(x.id)}">${copySvg()}<span>Copy</span></button></article>`).join('');empty.hidden=!!list.length;grid.querySelectorAll('.copy').forEach(b=>b.onclick=()=>doCopy(b.dataset.id,b))}
async function doCopy(id,b){let x=defaultPrompts.find(p=>p.id===id);if(!x)return;try{await navigator.clipboard.writeText(x.prompt)}catch{let t=document.createElement('textarea');t.value=x.prompt;document.body.appendChild(t);t.select();document.execCommand('copy');t.remove()}let old=b.innerHTML;b.classList.add('copied');b.innerHTML=`<svg viewBox="0 0 24 24"><path d="m5 12 4 4L19 6"/></svg><span>Copied</span>`;toast.textContent='Prompt copied to clipboard';toast.classList.add('show');clearTimeout(timer);timer=setTimeout(()=>{toast.classList.remove('show');b.classList.remove('copied');b.innerHTML=old},1300)}
function setTheme(t){document.body.classList.toggle('dark',t==='dark');themeText.textContent=t==='dark'?'Light':'Dark'}setTheme(localStorage.getItem('promptClipboardTheme')==='dark'?'dark':'light');theme.onclick=()=>{let t=document.body.classList.contains('dark')?'light':'dark';localStorage.setItem('promptClipboardTheme',t);setTheme(t)};search.oninput=render;renderCats();render();
